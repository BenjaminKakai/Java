/*import static sun.management.counter.Variability.CONSTANT;

public class Constantplusdigit {
    private static int (int n) {
        int sum = 0;
        while (n > 0) {
            int digit = (n % 10) + CONSTANT; // Add a constant to each digit
            sum += digit;
            n /= 10;
        }

    }
}
*/