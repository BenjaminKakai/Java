/*public class AddingandSubtracting {
    int sum = 0;
    boolean add = true;
while (n > 0) {
        int digit = n % 10;
        sum += add ? digit : -digit;
        add = !add; // Toggle between adding and subtracting
        n /= 10;
    }

} */
